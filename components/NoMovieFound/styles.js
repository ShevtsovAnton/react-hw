import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  container: {
    margin: 'auto',
    fontSize: '3em',
    textAlign: 'center',
    padding: 100,
    background: '#232323'
  }
});

export default useStyles;
